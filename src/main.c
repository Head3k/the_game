#include<stdio.h>

int input(int enter);

int main() {
	int enter = 0;
	enter = input(enter);
	return 0;
}

int input(int enter) {
	int check_i = 0;
	char check_c ' ';
	check_i = scanf("%d%c", &enter, &check_c);
	checker(check_i, check_c);
}

void checker(int check_i, char check_c) {
	if (check_i != 2 || )
}
